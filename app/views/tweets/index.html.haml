= render "tweets/header"
.index
  .index__page
    - @tweets.each do |tweet|
      = link_to "/tweets/#{tweet.id}" do
        .index__page__tweet
          .index__page__tweet--name
            = tweet.user.nickname
          .index__page__tweet--text
            = tweet.text
= link_to new_tweet_path do
  .tweetbtn
    ツイートする